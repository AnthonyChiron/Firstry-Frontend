import { FormatDateJJMMDirective } from './format-date-jjmm.directive';

describe('FormatDateJJMMDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatDateJJMMDirective();
    expect(directive).toBeTruthy();
  });
});
